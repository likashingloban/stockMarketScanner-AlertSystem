# 🚀 Quick Start Guide

快速啟動指南 - 5 分鐘內運行專案

---

## ✅ 前置準備

確保你已安裝:
- Python 3.x
- 瀏覽器 (Chrome/Firefox/Safari)

---

## 📝 步驟 1: 啟動後端

打開終端，執行以下命令:

```bash
# 進入後端資料夾
cd backend

# 安裝依賴
pip install -r requirements.txt

# 初始化資料庫
python database.py

# 啟動 API 伺服器
python app.py
```

✅ 看到以下訊息表示成功:
```
🚀 Flask API Server starting...
📍 API endpoint: http://localhost:5000
```

**保持此終端運行**，不要關閉！

---

## 📝 步驟 2: 啟動前端

打開**新的終端視窗**，執行:

```bash
# 進入前端資料夾
cd frontend

# 啟動 HTTP 伺服器
python -m http.server 8080
```

✅ 看到以下訊息表示成功:
```
Serving HTTP on :: port 8080 (http://[::]:8080/) ...
```

---

## 📝 步驟 3: 開啟瀏覽器

打開瀏覽器訪問:
```
http://localhost:8080
```

---

## 📝 步驟 4: 註冊帳戶並測試

1. **註冊新帳戶**:
   - 點擊 "立即註冊"
   - 填寫用戶名、郵箱、密碼
   - 提交

2. **登入系統**:
   - 使用剛才註冊的帳戶登入

3. **新增股票**:
   - 點擊 "Watchlist"
   - 輸入股票代碼 (例如: `AAPL`, `TSLA`, `GOOGL`)
   - 點擊 "新增股票"

4. **更新股價數據**:
   - 打開**新的終端視窗**
   - 執行:
     ```bash
     cd backend
     python fetch_stock_data.py
     ```
   - 選擇 `1` (使用模擬數據)
   - 等待數據更新完成

5. **查看數據**:
   - 回到瀏覽器
   - 刷新頁面 (F5)
   - 現在應該能看到股價數據了！

6. **查看股票詳情**:
   - 點擊任意股票的 "詳情" 按鈕
   - 查看價格走勢圖表

7. **設定提醒**:
   - 在股票詳情頁面
   - 設定價格提醒 (例如: 價格高於 $180)
   - 點擊 "設定提醒"

---

## 🎯 完整流程示意圖

```
┌─────────────────┐
│  註冊/登入      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  新增股票       │  ← 輸入 AAPL, TSLA, GOOGL
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  執行數據腳本   │  ← python fetch_stock_data.py
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  查看 Dashboard │  ← 刷新頁面
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  查看詳情/圖表  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  設定提醒       │
└─────────────────┘
```

---

## ❓ 常見問題

### Q: 登入失敗?
**A**: 確保後端 API 正在運行 (`http://localhost:5000`)

### Q: 看不到股價數據?
**A**: 執行數據腳本 `python fetch_stock_data.py`

### Q: 出現 CORS 錯誤?
**A**: 確保:
1. 後端已安裝 `flask-cors`
2. 前端使用 HTTP 伺服器 (不要直接雙擊 HTML)

### Q: 圖表不顯示?
**A**:
1. 確保已執行數據腳本生成歷史數據
2. 檢查瀏覽器 Console 是否有錯誤

---

## 📚 下一步

- 查看 `ARCHITECTURE.md` 了解架構細節
- 查看 `PRD.md` 了解完整功能需求
- 開始客製化功能！

---

## 🎉 成功！

如果以上步驟都順利完成，恭喜你已經成功運行了完整的前後端分離專案！

需要幫助? 查看 `README.md` 或 `ARCHITECTURE.md`
